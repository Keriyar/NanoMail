# NanoMail - OAuth2 配置示例
#
# 将此文件重命名为 `.env` 并填入您的 Google OAuth2 凭据

# Gmail OAuth2 客户端 ID（从 Google Cloud Console 获取）
# https://console.cloud.google.com/apis/credentials
GMAIL_CLIENT_ID=YOUR_CLIENT_ID.apps.googleusercontent.com

# Gmail OAuth2 客户端密钥
GMAIL_CLIENT_SECRET=YOUR_CLIENT_SECRET

# 可选：自定义 OAuth2 重定向 URI（默认: http://localhost:8080）
# OAUTH_REDIRECT_URI=http://localhost:8080

# 可选：日志级别（trace, debug, info, warn, error）
# RUST_LOG=nanomail=debug,info
